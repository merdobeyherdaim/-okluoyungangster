def tavla(update, context):
    update.message.reply_text('Tavla başlatılıyor!')

def satranc(update, context):
    update.message.reply_text('Satranç başlatılıyor!')

def oyunlar(update, context):
    update.message.reply_text('Mevcut oyunlar: Tavla, Satranç')
